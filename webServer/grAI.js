const url = 'http://localhost:3000';

const videoInput = document.getElementsByClassName('videoInput');
const canvasElement = document.getElementsByClassName('output_canvas')[0];
const availableActivation = false;

function startGR() {
    console.log("HA")
}
